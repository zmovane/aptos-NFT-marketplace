{
  "name": "market-contracts",
  "version": "0.1.0",
  "scripts": {
    "compile": "aptos move compile --package-dir . --named-addresses $NAME=$ADDRESS",
    "publish": "aptos move publish --package-dir . --named-addresses $NAME=$ADDRESS"
  }
}